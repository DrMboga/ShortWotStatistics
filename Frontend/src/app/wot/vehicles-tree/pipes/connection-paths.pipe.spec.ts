import { ConnectionPathsPipe } from './connection-paths.pipe';

describe('ConnectionPathsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConnectionPathsPipe();
    expect(pipe).toBeTruthy();
  });
});
