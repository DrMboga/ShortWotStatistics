import { FrameHeightPipe } from './frame-height.pipe';

describe('FrameHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new FrameHeightPipe();
    expect(pipe).toBeTruthy();
  });
});
