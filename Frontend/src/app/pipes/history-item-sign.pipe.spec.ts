import { HistoryItemSignPipe } from './history-item-sign.pipe';

describe('HistoryItemSignPipe', () => {
  it('create an instance', () => {
    const pipe = new HistoryItemSignPipe();
    expect(pipe).toBeTruthy();
  });
});
