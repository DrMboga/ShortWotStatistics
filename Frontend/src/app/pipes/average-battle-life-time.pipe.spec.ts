import { AverageBattleLifeTimePipe } from './average-battle-life-time.pipe';

describe('AverageBattleLifeTimePipe', () => {
  it('create an instance', () => {
    const pipe = new AverageBattleLifeTimePipe();
    expect(pipe).toBeTruthy();
  });
});
