import { AchievementInfoPipe } from './achievement-info.pipe';

describe('AchievementInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new AchievementInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
