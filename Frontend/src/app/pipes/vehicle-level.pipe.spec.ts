import { VehicleLevelPipe } from './vehicle-level.pipe';

describe('VehicleLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
