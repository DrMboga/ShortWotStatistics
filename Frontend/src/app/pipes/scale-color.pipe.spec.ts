import { ScaleColorPipe } from './scale-color.pipe';

describe('ScaleColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ScaleColorPipe();
    expect(pipe).toBeTruthy();
  });
});
