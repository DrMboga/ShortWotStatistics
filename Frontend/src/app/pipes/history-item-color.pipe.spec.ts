import { HistoryItemColorPipe } from './history-item-color.pipe';

describe('HistoryItemColorPipe', () => {
  it('create an instance', () => {
    const pipe = new HistoryItemColorPipe();
    expect(pipe).toBeTruthy();
  });
});
